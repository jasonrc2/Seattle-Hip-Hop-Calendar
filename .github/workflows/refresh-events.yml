name: Refresh Seattle Hip-Hop events

on:
  schedule:
    - cron: "17 15 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "Event collector goes here."
          echo "Connect approved public sources and update public/events.json."
      # Once the collector is implemented:
      # - run: python scripts/collect_events.py
      # - run: |
      #     git config user.name "event-bot"
      #     git config user.email "actions@github.com"
      #     git add public/events.json
      #     git diff --cached --quiet || git commit -m "Refresh events"
      #     git push
